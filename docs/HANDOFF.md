# HANDOFF — KnightTreasure (single overwritten file)

> Replaces date-stamped handoffs (DISPATCH.md §0). Overwritten each handoff; git history
> keeps prior versions.

## Read first

1. `docs/BACKLOG.md` — the queue (starter — needs proper seeding, see its `## Next`).
2. GDD v4.0 + the pivot decisions doc — the plan of record for the match-3 heist pivot.

## State of play (as of 2026-08-06 seeding)

- Phase 1+2 of the match-3 pivot committed and pushed (5 commits ending `e7bbca1`,
  2026-07-23): Vite/TS/PixiJS toolchain, pure logic module (26 unit tests), renderer + HUD.
- Phases 3–6 are stubs. `tile_wildcard.png` role still undecided.

## Ready-to-paste next-session prompt

```
Continue KnightTreasure. Read docs/BACKLOG.md first, then GDD v4.0. First task: seed the
backlog properly from the GDD and LOOP-STATE.md, then ask the owner which phase is next.
```
