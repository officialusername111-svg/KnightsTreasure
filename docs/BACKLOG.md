# BACKLOG

> Canonical pending state (DISPATCH.md §0 "The standing queue"). Entries are candidates —
> re-derive from the repo at run start. Single writer: the orchestrating session only.
> STARTER seeded 2026-08-06 by the toolkit-overhaul run: the queue content below is
> deliberately thin — the next working session should seed `## Next` from the GDD v4.0,
> the pivot decisions doc, and the root `LOOP-STATE.md` (if its rows are still live).

## Now

(idle — no active run)

## Next

- [agent-derived] Seed this backlog properly: reconcile GDD v4.0 phases (Phase 3+ of the
  match-3 pivot are unstarted per the 2026-07-23 close-out), the root LOOP-STATE.md rows,
  and the `tile_wildcard.png` unassigned-role open question into real queue items.

## Parked

- Push-size hazard: binary asset history makes pushes exceed 2 minutes (2026-07-23 run) —
  optional one-time LFS migration is an ASK-tier decision for the owner.

## Decided — do not reopen

- 2026-07-23 — the memory-match build is archived; the project is the match-3 heist pivot
  (pivot decisions doc + GDD v4.0).

## Standing

(CLAUDE.md is authoritative; mirror what matters here as it accumulates)

- Commits go straight to main; never push without asking.
- Generated game assets are produced by the owner externally (Google Flow etc.) and dropped
  into the repo — list needed assets with prompts instead of generating placeholders.
